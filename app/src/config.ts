export const PRIVACY_MODE: boolean = true;

// To change this for a build, you would manually edit this file before building,
// or use a script to do so as part of the CI/CD process.
// For more advanced scenarios, consider EAS Build profiles or a dedicated config library. 